export type themeType = {
  colors: {
    input_background: string;
    text_color: string;
    button_background: string;
    button_text: string;
    application_background: string;
  };

  font_family: {
    regular: string;
    bold: string;
  };

  font_size: {
    xs: number;
    sm: number;
    md: number;
    lg: number;
    xl: number;
    xxl: number;
    xxxl: number;
  };
};
