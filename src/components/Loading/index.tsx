import React from 'react';
import { Container, LoadingElement } from './styles';

const Loading = () => {
  return (
    <Container>
      <LoadingElement size={'large'} />
    </Container>
  );
};

export default Loading;
